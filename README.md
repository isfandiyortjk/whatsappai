# Assistant Doner Home (WhatsApp + OpenAI)

WhatsApp-бот на русском с двумя ролями:
- 🤵 Рабочий ассистент (для сотрудников: смены, отчёты, питание)
- 🧠 Личный управляющий (для владельца: рассылки, статистика, добавление сотрудников)

## 🚀 Деплой в 1 клик (Render)
1) Создай новый репозиторий в GitHub и залей эти файлы.
2) На https://render.com — **New → Web Service → Connect Repo**.
3) Укажи:
   - Build Command: `npm install`
   - Start Command: `npm start`
4) Добавь переменные окружения:
   - `OPENAI_KEY` — ключ OpenAI
   - `META_VERIFY_TOKEN` — придуманный токен проверки
   - `META_WA_TOKEN` — токен Meta (временно: из API Setup)
   - `META_PHONE_NUMBER_ID` — ID номера из API Setup
   - `ADMIN_PHONE` — телефон владельца (с + или без, формат не критичен)
   - `STAFF_PHONES` — телефоны сотрудников через запятую
5) Задеплой и получи URL: `https://<app>.onrender.com`

## 🔌 Подключение Webhook в Meta
- В Meta Developers → App → WhatsApp → Configuration:
  - Callback URL: `https://<app>.onrender.com/webhook`
  - Verify token: тот же, что в `META_VERIFY_TOKEN`
  - Подпишись на `messages`.
- В API Setup возьми:
  - Temporary Access Token → в `META_WA_TOKEN`
  - Phone Number ID → в `META_PHONE_NUMBER_ID`

## 🧭 Команды
**Сотрудник:**
- `смена старт` — начать смену
- `смена стоп` — завершить смену
- `отчёт: ...` — отчёт за смену
- `питание: ...` — что ел/пил
- `статус` — текущий статус

**Руководитель (номер из ADMIN_PHONE):**
- `рассылка: текст` — отправить всем сотрудникам из STAFF_PHONES
- `статистика` — краткая статистика
- `добавить: +491234567890` — добавить сотрудника (в память процесса)

## ⚠️ Примечания
- Хранилище сейчас в памяти (демо). Для продакшена подключите БД или Google Sheets.
- Токен Meta из API Setup действует ~23 часа. Для постоянного токена создайте System User и выдайте права.
- Добавьте телефоны администратора и сотрудников в **Allowed recipients** (Meta Developers → WhatsApp → API Setup → **Add phone number**), иначе Meta вернёт ошибку `(#131030) Recipient phone number not in allowed list`.
- Попросите каждого сотрудника сначала написать боту, чтобы открыть 24-часовой диалог; без этого Meta заблокирует исходящие сообщения.
- Если `npm install` даёт сетевые ошибки, можно временно:
  ```
  npm config set registry https://registry.npmmirror.com
  npm cache clean --force
  npm install
  ```

## ✅ Проверка
- Отправь сообщение с телефона сотрудника: `смена старт` → бот ответит.
- С номера владельца: `рассылка: Привет команде!` → все сотрудники получат сообщение.

Удачи! 💙